# FrameLoom

FrameLoom is a local desktop archive for storyboard assets. It supports durable JSON workspaces, validated geometry imports, isolated query snapshots, rendering bounds, tag filters, summaries, staged exports, and an event log. The command starts an archive at a requested JSON path; reopen the same JSON file to inspect persisted shots.
